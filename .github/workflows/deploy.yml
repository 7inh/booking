name: deploy
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: yarn

      - name: Build app
        run: yarn build

      - name: Deploy
        uses: actions/deploy-to-github-pages@v7
        with:
          github_token: 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
          publish_branch: gh-pages
          publish_dir: ./dist
