export enum RareEnum {
    normal = 1,
    special = 2,
    limited = 3,
    collection = 4,
}

export enum VariantEnum {
    once = 1,
    combo = 2,
    fullSet = 3,
}

export enum AvailabilityEnum {
    preOrder = 1,
    available = 2,
}
